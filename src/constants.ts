/**
 * This prefix is used for the localStorage key
 */
export const PREFIX = 'rps'

/**
 * How many seconds should the player have to choose a weapon?
 */
export const COUNTDOWN_SECONDS = 3

export const SECOND_IN_MS = 1000
